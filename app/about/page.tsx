import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'About',
    description: 'About page',
};

const About = () => {
    return (
        <div>
            <h1>About</h1>
        </div>
    )
};

export default About;
